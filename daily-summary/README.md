# Daily Summary - AI 每日工作总结助理

一个 Claude Code 插件，渐进式总结当日对话记录，生成包含知识沉淀与导师建议的高质量工作总结。

## 功能特点

- **渐进式总结** - 分层处理对话数据，逐步精炼，避免信息过载
- **双视角输出** - 智能助理（回顾）+ 成长导师（前瞻）
- **知识导向** - 按知识主题组织，而非机械地按项目/时间分类
- **Git 集成** - 自动提交到配置的 Git 仓库，便于版本管理和回顾

## 安装

插件已包含在 `cheafon-plugins` marketplace 中，启用后自动可用。

## 首次使用

### 1. 配置 Git 仓库

首次使用前需要配置工作总结的存储仓库：

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/setup-git.sh
```

脚本会引导你：
1. 输入本地 Git 仓库路径
2. 配置远程仓库
3. 测试推送权限
4. 保存配置到 `~/.claude/daily-summary.local.md`

### 2. 使用命令

```
/daily-summary                        # 全面总结当天对话
/daily-summary TypeScript 类型系统    # 聚焦特定主题
/daily-summary 今天遇到的 bug         # 聚焦问题与解决
```

## 输出格式

```markdown
# 每日总结 - 2025-12-25

> 一句话总结今天的主题/收获

## 📚 今日学习收获
### [主题1]
- 要点描述
- 关键代码/命令示例

## 🔧 问题与解决
### [问题简述]
- **问题**：具体描述
- **解决**：解决方案
- **启发**：从中学到什么

## 💡 关键洞察
- 值得长期记住的知识点或最佳实践

## 🎯 导师点评
对今天工作的整体评价，语气鼓励但客观

## 🚀 下一步行动
1. 具体可执行的下一步任务
2. 需要深入学习的领域
```

## 目录结构

```
daily-summary/
├── .claude-plugin/
│   └── plugin.json           # 插件清单
├── commands/
│   └── daily-summary.md      # 主命令
├── agents/
│   └── summary-generator.md  # 总结生成 Agent
├── scripts/
│   ├── parse-conversations.py  # 对话解析脚本
│   ├── setup-git.sh            # Git 配置向导
│   └── git-commit.sh           # Git 提交脚本
└── README.md
```

## 配置文件

配置存储在 `~/.claude/daily-summary.local.md`：

```yaml
---
git_repo: /path/to/your/summary-repo
git_remote: origin
git_branch: main
---
```

## 工作原理

```
/daily-summary [引导]
       │
       ▼
  检查配置 (~/.claude/daily-summary.local.md)
       │
       ▼
  parse-conversations.py
  (扫描 ~/.claude/projects，提取当天对话)
       │
       ▼
  summary-generator agent
  (渐进式总结：提取 → 聚类 → 双视角精炼)
       │
       ▼
  写入 /tmp/daily-summary/YYYY-MM-DD.md
       │
       ▼
  git-commit.sh
  (复制到仓库 → commit → push)
```

## 常见问题

### Q: 提示"未找到配置"？
运行 `bash ${CLAUDE_PLUGIN_ROOT}/scripts/setup-git.sh` 进行配置。

### Q: Git 每次都要输入密码？
配置 credential helper：
```bash
git config --global credential.helper store
```
然后下次输入一次密码后会被永久保存。

### Q: 如何修改 Git 仓库配置？
直接编辑 `~/.claude/daily-summary.local.md` 或重新运行 setup-git.sh。

## 版本历史

- **1.0.0** - 初始版本
  - `/daily-summary` 命令
  - 渐进式总结 Agent
  - Git 自动提交
  - 可选引导参数

## 许可证

MIT
